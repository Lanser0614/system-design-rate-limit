services:
    redis:
        container_name: radis_system_design
        image: 'bitnami/redis:6.2'
        environment:
            - ALLOW_EMPTY_PASSWORD=yes
        ports:
            - "6379:6379"
